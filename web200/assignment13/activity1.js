window.onload = function () {
    document.getElementById("myBtn").addEventListener("click", returnText);
}

/* 





ignore... tried to add an error message at the bottom of my code and failed






Create an HTML form that allows the user to enter their 
first name, last name, address, city, region, postal code, 
email address, phone number, and date of birth.

   let x = document.getElementById("firstName")
   let y = document.getElementById("lastName")
   let form = document.getElementById("myForm")
   let errorElement = document.getElementById("error")

   form.addEventListener('submit', (e) => {
   let messages = []

    if (x.value === "" || x.value == null) {
        messages.push('First name is required.')
        return false;
    } 
    
    if (y.value === "" || y.value == null ) {
        messages.push('Last name is required.')
        return false;
    } 

    if (messages.length > 0){
        e.preventDefault()
        errorElement.innerText = messages.join(', ')
    }


})
*/


